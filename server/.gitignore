node_modules/
dist/
.env
.env.local
.env.*.local
*.log

# Prisma's generated client — lives outside src/ so the same relative import
# ('../generated/prisma') resolves from both src/ and dist/. Regenerated by
# `prisma generate` (which the postinstall script runs).
generated/

# Local dev keypair from scripts/dev-token.ts — never commit.
.dev-keys.json
